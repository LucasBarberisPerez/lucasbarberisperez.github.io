---
import { SUPPORTED_LANGUAGES } from "../i18n/translations";
const lang = navigator.language.slice(0, 2);
const isSupported = SUPPORTED_LANGUAGES.includes(lang);

if (isSupported) {
  return Astro.redirect(`/${lang}/`);
}

return Astro.redirect(`/en/`);
---
